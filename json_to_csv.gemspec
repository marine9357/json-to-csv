Gem::Specification.new do |s|
  s.name = %q{json_to_csv}
  s.version = "0.0.0"
  s.authors = ["Marine"]
  s.date = %q{2021-05-13}
  s.summary = %q{json_to_csv allows you to convert JSON files composed of arrays of objects (all following the same schema) to a CSV file where one line equals one object.}
  s.files = [
    "lib/json_to_csv.rb"
  ]
  s.require_paths = ["lib"]
end